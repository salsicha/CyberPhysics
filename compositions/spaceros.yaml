
# SpaceROS

services:
  spaceros:
    network_mode: "host"
    image: cyberphysics/spaceros
    container_name: spaceros
    environment:
      - "DISPLAY=${DISPLAY}"
      - XAUTHORITY=/tmp/.XAuthority
    volumes:
      - "/tmp/.X11-unix:/tmp/.X11-unix"
      - /dev/shm:/dev/shm
      - ${XAUTHORITY:-/tmp/.Xauthority}:/tmp/.XAuthority:ro
    command: >
      stdbuf -o L
      /demos/curiosity_rover/run.sh
      